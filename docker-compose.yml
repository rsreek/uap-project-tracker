services:
  uap-tracker:
    build: .
    container_name: uap-project-tracker
    ports:
      - "2255:2255"
    volumes:
      - ./data:/app/data
      - /opt/uap-project-tracker/certs:/opt/certs:ro
    restart: unless-stopped
